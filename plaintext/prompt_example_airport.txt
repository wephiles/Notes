-------------------------------------------------------------------------
我想开发一个简单的桌面 GUI 程序，用于去除文本中的所有换行符。请帮我编写完整的代码。

具体需求如下：

技术栈：
使用 Python 语言。
使用 Tkinter 作为 GUI 库（因为它是 Python 内置的，无需额外安装）。
界面设计：
窗口标题为“换行符去除工具”。
窗口大小设置为 600x400，并允许调整大小。
上方有一个多行文本输入框，带有标签“输入文本”。
中间放置两个按钮：
按钮“去除换行符”：执行核心逻辑。
按钮“复制结果”：将处理后的文本复制到系统剪贴板。
下方有一个多行文本输出框，带有标签“输出结果”，用于显示处理后的文本。
界面布局要整洁，使用 grid 或 pack 布局管理器均可。
核心功能：
点击“去除换行符”按钮后，程序应读取上方输入框的内容。
去除所有的换行符（包括 \n 和 \r），将文本合并为一行。
将处理后的结果显示在下方的输出框中。
点击“复制结果”按钮时，将输出框的内容复制到系统剪贴板，并弹窗提示“复制成功”。
交付物：
请提供完整的单个 Python 脚本文件代码。
代码中需要包含必要的注释。
-------------------------------------------------------------------------
现在请帮我将刚才生成的 Python GUI 程序打包成 Windows 可执行文件（.exe）。

具体要求如下：

工具选择：
使用 PyInstaller 进行打包。
如果当前环境未安装 PyInstaller，请先帮我安装它。
打包参数：
使用 --onefile 参数：将所有依赖打包成一个单独的 .exe 文件。
使用 --noconsole 参数：运行程序时不显示黑色的命令行窗口（这是 GUI 程序的标配）。
使用 --clean 参数：清理临时文件，避免缓存问题。
执行方式：
请直接在终端中执行打包命令。
打包完成后，请告诉我生成的 .exe 文件位于哪个目录下（通常是 dist 文件夹）。

-------------------------------------------------------------------------
大米
地址发布页
http://pub.bigmeok.me
https://1s.bigmeok.me

国内无法访问-永久域名
https://user.bigme.pro

国内可访问-临时域名
https://portal.bigmess.org
https://app.bigme.online
https://cloudflare.bigmess.org
以上官网，哪个都可以进入网站，大家请收藏好
请复制到自带浏览器打开，不要国产软件打开
收藏好！收藏好！收藏好！要不然很容易失联！
请牢记自己的网站账号，自己购买梯子用的网站名字
